@*@model Application.ViewModels.Client.ClientViewModel

@{
    bool editMode = Model.Id == 0 ? false : true;
    Layout = null;
}

<div class="container-fluid">
    <div class="row">
        <div class="col-7 offset-sm-3">
            <div class="card">
                <div class="card-title bg-dark fw-bold text-light">
                    <div class="text-center">@(editMode ? $"Editando el cliente {Model.ClientName}" : "Creando un cliente")</div>
                </div>
                <div class="card-body">

                    <form asp-controller="Client" asp-action="@(editMode ? "Update" : "Create")">
                                                
                        <div class="mb-3">
                            <label asp-for="ClientName" class="form-label">Nombre del cliente</label>
                            <input asp-for="ClientName" class="form-control" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="ClientDirection" class="form-label">Dirección del cliente</label>
                            <input asp-for="ClientDirection" class="form-control" />
                        </div>

                        <input type="hidden" asp-for="Id" />

                        <input type="submit" value="@(editMode ? "Editar" : "Guardar")" class="btn btn-primary" />
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>*@